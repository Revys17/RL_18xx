[project]
name = "rl18xx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.1.1",
    "torch>=2.6.0",
]

[tool.pytest.ini_options]
addopts = "--tb=line"
